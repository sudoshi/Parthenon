# =============================================================================
# Acropolis — Base Infrastructure
# =============================================================================
# This file defines the shared network and Traefik reverse proxy.
# Layer community or enterprise compose files on top:
#
#   Community:   docker compose -f docker-compose.yml -f docker-compose.community.yml up -d
#   Enterprise:  docker compose -f docker-compose.yml -f docker-compose.community.yml -f docker-compose.enterprise.yml up -d
#
# Or use the CLI:  ./acropolis.sh up --edition community|enterprise
# =============================================================================

services:

  # ── Traefik — Reverse Proxy + Auto SSL ────────────────────────────────────
  traefik:
    container_name: acropolis-traefik
    image: traefik:v3.6
    restart: unless-stopped
    command:
      - --configFile=/etc/traefik/traefik.yml
    ports:
      - "${TRAEFIK_HTTP_PORT:-80}:80"
      - "${TRAEFIK_HTTPS_PORT:-443}:443"
      - "${TRAEFIK_DASHBOARD_PORT:-8090}:8080"   # Dashboard (internal only)
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - ./traefik/dynamic:/etc/traefik/dynamic:ro
      - ./traefik/acme:/etc/traefik/acme
    environment:
      - TZ=${TZ:-America/New_York}
    networks:
      - acropolis
      - parthenon
    healthcheck:
      test: ["CMD", "traefik", "healthcheck", "--ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    labels:
      - "traefik.enable=true"

networks:
  # Ingress network — shared with Alfresco, OpenProject, parthenon-php.
  # Declared external because the acumenus network is created by Acumenus
  # internal compose projects (alfresco stack), not by Acropolis.
  acropolis:
    external: true
    name: acumenus
  # Backend tier — DB, Redis, Kafka, OpenSearch, Celery workers live here
  # and are NOT reachable from acumenus. internal: true disables NAT.
  # Only ingress services (authentik-server, superset, datahub-frontend,
  # wazuh-dashboard) sit on both acumenus and backend.
  backend:
    name: acropolis-backend
    driver: bridge
    internal: true
  parthenon:
    external: true
    name: parthenon

volumes:
  traefik_acme:
