apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: acropolis

resources:
  - ../base
  # Enterprise services deployed via Helm chart with enterprise values
  # kubectl apply -k k8s/kustomize/enterprise/

commonLabels:
  acropolis.io/edition: enterprise

# Enterprise-specific patches
# patches:
#   - path: patches/enterprise-resources.yaml
