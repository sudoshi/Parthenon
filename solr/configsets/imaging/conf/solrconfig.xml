<?xml version="1.0" encoding="UTF-8" ?>
<config>
  <luceneMatchVersion>9.11.1</luceneMatchVersion>

  <dataDir>${solr.data.dir:}</dataDir>

  <directoryFactory name="DirectoryFactory" class="${solr.directoryFactory:solr.NRTCachingDirectoryFactory}"/>
  <codecFactory class="solr.SchemaCodecFactory"/>
  <schemaFactory class="ClassicIndexSchemaFactory"/>

  <indexConfig>
    <ramBufferSizeMB>64</ramBufferSizeMB>
    <maxBufferedDocs>500</maxBufferedDocs>
  </indexConfig>

  <updateHandler class="solr.DirectUpdateHandler2">
    <updateLog>
      <str name="dir">${solr.ulog.dir:}</str>
    </updateLog>
    <autoCommit>
      <maxTime>${solr.autoCommit.maxTime:10000}</maxTime>
      <openSearcher>false</openSearcher>
    </autoCommit>
    <autoSoftCommit>
      <maxTime>${solr.autoSoftCommit.maxTime:2000}</maxTime>
    </autoSoftCommit>
  </updateHandler>

  <!-- Main search handler with facets for imaging browse -->
  <requestHandler name="/select" class="solr.SearchHandler">
    <lst name="defaults">
      <str name="echoParams">explicit</str>
      <str name="wt">json</str>
      <int name="rows">50</int>
      <str name="df">text</str>
      <str name="defType">edismax</str>
      <str name="qf">study_description^3 patient_name_dicom^2 series_descriptions^2 modality^1 body_part_examined^1</str>
      <str name="pf">study_description^3</str>
      <!-- Facets for study browser filtering -->
      <str name="facet">true</str>
      <str name="facet.field">modality</str>
      <str name="facet.field">body_part_examined</str>
      <str name="facet.field">status</str>
      <str name="facet.field">has_measurements</str>
      <str name="facet.range">study_date</str>
      <str name="facet.range.start">NOW/YEAR-10YEARS</str>
      <str name="facet.range.end">NOW</str>
      <str name="facet.range.gap">+1YEAR</str>
      <str name="facet.mincount">1</str>
      <!-- Highlighting -->
      <str name="hl">true</str>
      <str name="hl.fl">study_description,series_descriptions,patient_name_dicom</str>
      <str name="hl.simple.pre">&lt;mark&gt;</str>
      <str name="hl.simple.post">&lt;/mark&gt;</str>
    </lst>
  </requestHandler>

  <!-- Suggest handler for autocomplete -->
  <searchComponent name="suggest" class="solr.SuggestComponent">
    <lst name="suggester">
      <str name="name">studySuggester</str>
      <str name="lookupImpl">AnalyzingInfixLookupFactory</str>
      <str name="dictionaryImpl">DocumentDictionaryFactory</str>
      <str name="field">study_description_suggest</str>
      <str name="suggestAnalyzerFieldType">text_suggest</str>
      <str name="buildOnCommit">true</str>
    </lst>
  </searchComponent>

  <requestHandler name="/suggest" class="solr.SearchHandler" startup="lazy">
    <lst name="defaults">
      <str name="suggest">true</str>
      <str name="suggest.count">10</str>
      <str name="suggest.dictionary">studySuggester</str>
      <str name="wt">json</str>
    </lst>
    <arr name="components">
      <str>suggest</str>
    </arr>
  </requestHandler>

  <!-- Update handler -->
  <requestHandler name="/update" class="solr.UpdateRequestHandler"/>

  <!-- Admin ping -->
  <requestHandler name="/admin/ping" class="solr.PingRequestHandler">
    <lst name="invariants">
      <str name="q">*:*</str>
    </lst>
    <lst name="defaults">
      <str name="echoParams">all</str>
    </lst>
  </requestHandler>

  <!-- Query caches -->
  <query>
    <maxBooleanClauses>${solr.max.booleanClauses:1024}</maxBooleanClauses>
    <filterCache class="solr.CaffeineCache" size="128" initialSize="64" autowarmCount="0"/>
    <queryResultCache class="solr.CaffeineCache" size="128" initialSize="64" autowarmCount="0"/>
    <documentCache class="solr.CaffeineCache" size="128" initialSize="64" autowarmCount="0"/>
    <enableLazyFieldLoading>true</enableLazyFieldLoading>
    <queryResultWindowSize>50</queryResultWindowSize>
    <queryResultMaxDocsCached>200</queryResultMaxDocsCached>
  </query>

</config>
