---
phase: 02-fhir-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/administration/pages/FhirExportPage.tsx
  - frontend/src/features/administration/api/fhirExportApi.ts
autonomous: true
requirements:
  - FHIR-01
  - FHIR-02

must_haves:
  truths:
    - "Navigating to /admin/fhir-export renders a clean page with no runtime errors"
    - "Page displays a clear coming soon message explaining FHIR Bulk Export is not yet available"
    - "No API calls are made to nonexistent backend endpoints"
  artifacts:
    - path: "frontend/src/features/administration/pages/FhirExportPage.tsx"
      provides: "Coming soon page component replacing broken export form"
      min_lines: 15
    - path: "frontend/src/features/administration/api/fhirExportApi.ts"
      provides: "Emptied API file with types preserved for future use"
  key_links:
    - from: "frontend/src/app/router.tsx"
      to: "frontend/src/features/administration/pages/FhirExportPage.tsx"
      via: "lazy route import at path fhir-export"
      pattern: "fhir-export"
---

<objective>
Replace the broken FHIR Export page with a clean "coming soon" state.

Purpose: The current FhirExportPage calls POST /fhir/$export and GET /fhir/$export/{id} -- backend endpoints that do not exist. This causes runtime errors when the page is used. Since this is a bugfix project (not a feature project), the correct fix is to disable the broken UI with a graceful "coming soon" message rather than building the entire FHIR Bulk Export backend.

Output: A FHIR Export page that loads without errors and clearly communicates the feature is not yet available. The route stays intact so bookmarks are not broken.
</objective>

<execution_context>
@/home/smudoshi/.claude/get-shit-done/workflows/execute-plan.md
@/home/smudoshi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend/src/features/administration/pages/FhirExportPage.tsx
@frontend/src/features/administration/api/fhirExportApi.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace FhirExportPage with coming-soon state</name>
  <files>frontend/src/features/administration/pages/FhirExportPage.tsx</files>
  <action>
Rewrite FhirExportPage.tsx to display a "coming soon" placeholder instead of the broken export form. The new component must:

1. Remove ALL imports and usage of useStartFhirExport and useFhirExportStatus hooks (these call nonexistent endpoints)
2. Remove all state variables (sourceId, selectedTypes, activeExportId) and handlers -- none are needed
3. Keep the page header style consistent with the existing dark clinical theme (#0E0E11 base, #F0EDE8 text, #2DD4BF teal accent, #8A857D muted text)
4. Display:
   - Page title: "FHIR Bulk Export" (same as current)
   - Subtitle: same as current
   - A centered card with a PackageOpen icon (already imported from lucide-react), "Coming Soon" heading, and a brief explanation: "FHIR Bulk Export ($export) is under development. This feature will allow exporting OMOP CDM data as FHIR R4 NDJSON files for interoperability."
   - A muted note: "The backend endpoints for this feature have not been implemented yet."
5. Use only lucide-react icons (PackageOpen) and Tailwind classes -- no new dependencies
6. Export as default function component (same pattern as current)

The component should be simple -- around 30-40 lines. No useState, no useEffect, no API calls.
  </action>
  <verify>
    <automated>cd /home/smudoshi/Github/Parthenon/frontend && npx tsc --noEmit --pretty 2>&1 | grep -E "FhirExport|error" | head -20</automated>
  </verify>
  <done>FhirExportPage.tsx compiles with zero TypeScript errors and makes zero API calls to nonexistent endpoints</done>
</task>

<task type="auto">
  <name>Task 2: Clean up fhirExportApi.ts to remove broken API calls</name>
  <files>frontend/src/features/administration/api/fhirExportApi.ts</files>
  <action>
Rewrite fhirExportApi.ts to remove the broken API call functions while preserving type definitions for future use:

1. Remove the apiClient import (no API calls remain)
2. Remove the useQuery, useMutation, useQueryClient imports from @tanstack/react-query
3. Remove functions: startFhirExport, getFhirExportStatus
4. Remove hooks: useStartFhirExport, useFhirExportStatus
5. KEEP the type/interface definitions (FhirExportFile, FhirExportJob, StartExportParams) -- these define the contract for when the backend is eventually built. Add a comment at the top: "// FHIR Bulk Export API types -- endpoints not yet implemented. See FhirExportPage.tsx coming-soon state."
6. Keep all exports on the types so they remain importable

The file should be around 20 lines -- just the type definitions with no runtime code.
  </action>
  <verify>
    <automated>cd /home/smudoshi/Github/Parthenon/frontend && npx tsc --noEmit --pretty 2>&1 | grep -E "fhirExport|error" | head -20</automated>
  </verify>
  <done>fhirExportApi.ts contains only type definitions, no runtime API calls, and compiles cleanly</done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes with no errors: `cd frontend && npx tsc --noEmit`
2. No references to nonexistent endpoints remain: `grep -r "fhir/\\\$export" frontend/src/ --include="*.ts" --include="*.tsx"` should return zero results from runtime code (only type comments if any)
3. The route in router.tsx still lazy-imports FhirExportPage (unchanged)
</verification>

<success_criteria>
- Navigating to /admin/fhir-export shows a styled "coming soon" page with no errors
- No network requests are made to /fhir/$export endpoints
- TypeScript compilation passes cleanly
- The router route is preserved (no broken bookmarks)
</success_criteria>

<output>
After completion, create `.planning/phases/02-fhir-export/02-01-SUMMARY.md`
</output>
