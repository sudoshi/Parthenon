---
phase: 01-project-setup-and-source-data-profiling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/irsf-etl/__init__.py
  - scripts/irsf-etl/__main__.py
  - scripts/irsf-etl/config.py
  - scripts/irsf-etl/requirements.txt
  - scripts/irsf-etl/pytest.ini
  - scripts/irsf-etl/lib/__init__.py
  - scripts/irsf-etl/lib/csv_utils.py
  - scripts/irsf-etl/lib/date_utils.py
  - scripts/irsf-etl/schemas/__init__.py
  - scripts/irsf-etl/schemas/person_characteristics.py
  - scripts/irsf-etl/schemas/medications.py
  - scripts/irsf-etl/tests/__init__.py
  - scripts/irsf-etl/tests/conftest.py
  - scripts/irsf-etl/tests/test_csv_utils.py
  - scripts/irsf-etl/tests/test_schemas.py
  - scripts/irsf-etl/tests/test_date_utils.py
  - .gitignore
autonomous: true
requirements:
  - FOUND-06
must_haves:
  truths:
    - "Running `python -m scripts.irsf_etl` from project root produces a help message showing available commands"
    - "pandera is installed and importable with a passing smoke test"
    - "Shared lib/ package is importable from any ETL script"
    - "Pandera schema validates sample Person_Characteristics data correctly"
  artifacts:
    - path: "scripts/irsf-etl/__main__.py"
      provides: "CLI entry point with help message"
      min_lines: 10
    - path: "scripts/irsf-etl/config.py"
      provides: "ETLConfig Pydantic Settings with source paths"
      contains: "ETLConfig"
    - path: "scripts/irsf-etl/lib/csv_utils.py"
      provides: "CSV reading utility with encoding handling"
      exports: ["read_csv_safe"]
    - path: "scripts/irsf-etl/lib/date_utils.py"
      provides: "Split-date column detection stub"
      exports: ["detect_split_date_columns"]
    - path: "scripts/irsf-etl/schemas/person_characteristics.py"
      provides: "Pandera schema for Person_Characteristics input"
      contains: "person_characteristics_schema"
    - path: "scripts/irsf-etl/tests/conftest.py"
      provides: "Shared test fixtures with sample CSV data"
      min_lines: 15
  key_links:
    - from: "scripts/irsf-etl/config.py"
      to: "external/2023 IRSF/OMOP/IRSF Dataset/"
      via: "Path objects in ETLConfig"
      pattern: "source_root.*external.*IRSF"
    - from: "scripts/irsf-etl/schemas/person_characteristics.py"
      to: "pandera"
      via: "DataFrameSchema definition"
      pattern: "pa\\.DataFrameSchema"
---

<objective>
Scaffold the Python ETL project at scripts/irsf-etl/ with shared lib/ package, pandera schemas, Pydantic config, test infrastructure, and CLI entry point.

Purpose: Establish the project structure that all 11 subsequent phases depend on. Every downstream ETL script imports from lib/ and schemas/.
Output: Working Python package with passing tests, importable modules, and CLI entry point.
</objective>

<execution_context>
@/home/smudoshi/.claude/get-shit-done/workflows/execute-plan.md
@/home/smudoshi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-and-source-data-profiling/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create project scaffold with config, lib stubs, and test infrastructure</name>
  <files>
    scripts/irsf-etl/__init__.py,
    scripts/irsf-etl/__main__.py,
    scripts/irsf-etl/config.py,
    scripts/irsf-etl/requirements.txt,
    scripts/irsf-etl/pytest.ini,
    scripts/irsf-etl/lib/__init__.py,
    scripts/irsf-etl/lib/csv_utils.py,
    scripts/irsf-etl/lib/date_utils.py,
    scripts/irsf-etl/schemas/__init__.py,
    scripts/irsf-etl/schemas/person_characteristics.py,
    scripts/irsf-etl/schemas/medications.py,
    scripts/irsf-etl/tests/__init__.py,
    scripts/irsf-etl/tests/conftest.py,
    scripts/irsf-etl/tests/test_csv_utils.py,
    scripts/irsf-etl/tests/test_schemas.py,
    scripts/irsf-etl/tests/test_date_utils.py,
    .gitignore
  </files>
  <behavior>
    - Test: `python -m scripts.irsf_etl` from project root exits 0 and prints help text containing "irsf-etl" and "profile"
    - Test: `from scripts.irsf_etl.lib import csv_utils, date_utils` succeeds
    - Test: `from scripts.irsf_etl.schemas import person_characteristics` succeeds
    - Test: csv_utils.read_csv_safe reads a valid CSV StringIO and returns a DataFrame
    - Test: csv_utils.read_csv_safe handles encoding errors gracefully (does not raise)
    - Test: date_utils.detect_split_date_columns finds known split-date column groups in a column list
    - Test: date_utils.detect_split_date_columns returns empty dict for columns with no date patterns
    - Test: person_characteristics_schema validates a valid sample row (participant_id, ChildsGender, ChildsDOB present)
    - Test: person_characteristics_schema rejects a row with duplicate participant_id
    - Test: medications_schema validates a valid sample row with MedRxNormCode column
  </behavior>
  <action>
    1. Create the full directory tree: scripts/irsf-etl/{lib,schemas,tests,output/profiles,output/staging,output/reports}
    2. Write requirements.txt with: pandera~=0.29, pydantic-settings~=2.0, rich~=13.0, pytest~=8.0, pytest-cov~=5.0, ruff~=0.9, mypy~=1.0
    3. Install deps: `source ai/.venv/bin/activate && pip install -r scripts/irsf-etl/requirements.txt`
    4. Write config.py with ETLConfig (Pydantic BaseSettings) containing:
       - source_root pointing to `external/2023 IRSF/OMOP/IRSF Dataset/` via Path
       - Properties: source_5201, source_5211, source_custom_extracts (all Path objects)
       - output_dir pointing to scripts/irsf-etl/output/
       - env_prefix = "IRSF_ETL_"
    5. Write lib/csv_utils.py with read_csv_safe(path, **kwargs) -> pd.DataFrame that:
       - Uses pd.read_csv with low_memory=False, encoding_errors='replace'
       - Replaces empty strings with pd.NA via df.replace('', pd.NA) -- critical per research pitfall 5
       - Returns the cleaned DataFrame
    6. Write lib/date_utils.py with:
       - SPLIT_DATE_PATTERNS dict mapping label to (month_col, day_col, year_col) tuples (all 6 patterns from research)
       - detect_split_date_columns(columns: list[str]) -> dict returning detected split-date groups
       - This is a STUB for Phase 1; full date assembly comes in Phase 2
    7. Write schemas/person_characteristics.py with pandera DataFrameSchema:
       - participant_id: str, nullable=False, unique=True
       - participant_id5201, participant_id5211: str, nullable=True
       - ChildsDOB: str, nullable=True, regex check for MM/DD/YY(YY)
       - ChildsGender: str, nullable=True, isin ["Male", "Female", ""]
       - diagnosis, mutation: str, nullable=True
       - coerce=True, strict=False (allows extra columns -- file has 66)
    8. Write schemas/medications.py with pandera DataFrameSchema:
       - participant_id: str, nullable=False
       - MedName: str, nullable=True
       - MedRxNormCode: str, nullable=True
       - coerce=True, strict=False
    9. Write __main__.py as CLI entry point using argparse with subcommands: "profile" (placeholder for Plan 02). Print help when no subcommand given.
    10. Write __init__.py files (empty for package recognition)
    11. Write pytest.ini with testpaths = tests, and conftest.py with fixtures providing sample CSV data as StringIO objects for Person_Characteristics and Medications
    12. Write tests following TDD (RED first, then implement): test_csv_utils.py, test_schemas.py, test_date_utils.py
    13. Add to .gitignore: scripts/irsf-etl/output/

    IMPORTANT: Use pathlib.Path throughout -- source data paths contain spaces ("2023 IRSF", "IRSF Dataset").
    IMPORTANT: Use `errors='replace'` for CSV reading per research pitfall on encoding.
    IMPORTANT: Do NOT use pydantic-settings Config inner class deprecated syntax. Use model_config = SettingsConfigDict(env_prefix="IRSF_ETL_") instead.
  </action>
  <verify>
    <automated>cd /home/smudoshi/Github/Parthenon && source ai/.venv/bin/activate && python -m scripts.irsf_etl --help && python -m pytest scripts/irsf-etl/tests/ -x -q</automated>
  </verify>
  <done>
    - `python -m scripts.irsf_etl` prints help with "profile" subcommand listed
    - All tests pass (csv_utils, date_utils, schemas)
    - lib/ modules importable: csv_utils.read_csv_safe, date_utils.detect_split_date_columns
    - schemas/ modules importable: person_characteristics.person_characteristics_schema, medications.medications_schema
    - ETLConfig resolves source paths correctly via Path objects
    - output/ directories exist and are gitignored
  </done>
</task>

</tasks>

<verification>
```bash
cd /home/smudoshi/Github/Parthenon
source ai/.venv/bin/activate

# 1. CLI entry point works
python -m scripts.irsf_etl --help

# 2. All tests pass
python -m pytest scripts/irsf-etl/tests/ -v

# 3. Imports work
python -c "from scripts.irsf_etl.lib import csv_utils, date_utils; from scripts.irsf_etl.schemas import person_characteristics, medications; from scripts.irsf_etl.config import ETLConfig; print('All imports OK')"

# 4. pandera smoke test
python -c "import pandera; print(f'pandera {pandera.__version__} OK')"

# 5. Config resolves paths
python -c "from scripts.irsf_etl.config import ETLConfig; c = ETLConfig(); print(f'Source root: {c.source_root}'); print(f'Exists: {c.source_root.exists()}')"
```
</verification>

<success_criteria>
- Running `python -m scripts.irsf_etl` from project root prints help with available commands
- pandera is installed and importable
- All test files pass with `pytest scripts/irsf-etl/tests/ -x`
- ETLConfig.source_root points to existing source data directory
- Shared lib/ and schemas/ are importable
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-and-source-data-profiling/01-01-SUMMARY.md`
</output>
