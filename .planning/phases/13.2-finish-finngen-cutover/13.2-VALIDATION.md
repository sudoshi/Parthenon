---
phase: 13.2
slug: finish-finngen-cutover
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-17
---

# Phase 13.2 — Validation Strategy

> Per-phase validation contract. Derived from `13.2-RESEARCH.md` §Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Pest 3.x + PHPUnit (R testthat optional, not mandatory for 13.2) |
| **Config file** | `backend/phpunit.xml` (APP_ENV=testing, DB=parthenon_testing, QUEUE=sync, MAIL=array) |
| **Quick run command** | `docker compose exec -T php vendor/bin/pest tests/Feature/FinnGen/EndpointGenerateCohortIdTest.php --no-coverage` |
| **Full suite command** | `docker compose exec -T php vendor/bin/pest tests/Feature/FinnGen tests/Unit/FinnGen --no-coverage` |
| **Estimated runtime** | ~45s quick / ~3min full suite / ~5min E2E runbook |

---

## Sampling Rate

- **Per task commit:** quick Pest subset (EndpointGenerateCohortIdTest + FinnGenSchemaExistsInTestDbTest)
- **Per wave merge:** full FinnGen Pest + Pint --test + PHPStan level 8
- **Phase gate:** full suite + manual E2E runbook (SC-5/6/7) + non-FinnGen regression + DEPLOY-LOG written
- **Max feedback latency:** 45s quick / 3min full

---

## Per-Task Verification Map

7 ROADMAP SCs → concrete test commands. Plan IDs assigned during planning.

| SC # | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 1 | TBD | 1 | — (invariant: preserves GENOMICS-12a) | — | R worker accepts both `finngen_endpoint_generation_id` and legacy `cohort_definition_id`; computes offset; rejects only if both null | smoke + E2E | `docker exec parthenon-darkstar Rscript -e 'source("/app/api/finngen/cohort_ops.R"); stopifnot(exists("finngen_endpoint_generate_execute"))'` → exit 0; full coverage via SC-5 | ❌ W0 (R source in repo; live via bind mount per RESEARCH §1) | ⬜ pending |
| 2 | TBD | 1 | — | — | Controller populates `finngen_endpoint_generation_id` in run params + keeps `cohort_definition_id => null` | Pest Feature | `pest tests/Feature/FinnGen/EndpointGenerateCohortIdTest.php` | ❌ W0 (new) | ⬜ pending |
| 3 | TBD | 1 | — | T-13.2-S1 (grant escalation) | Idempotent role-grants migration runs cleanly on fresh parthenon_testing | Pest + psql | `pest tests/Feature/Migrations/RoleSplitBaselineTest.php` (optional) + `psql -U claude_dev -h localhost parthenon_testing -Atc "SELECT tableowner FROM pg_tables WHERE schemaname='app' AND tablename='cohort_definitions'"` → `parthenon_migrator` | ❌ W0 (new migration); optional test | ⬜ pending |
| 4 | TBD | 2 | — | — | Full FinnGen Pest suite green on parthenon_testing | Pest (full) | `pest tests/Feature/FinnGen tests/Unit/FinnGen --no-coverage` → exit 0 | ✅ tests present | ⬜ pending |
| 5 | TBD | 3 | — (invariant) | — | PANCREAS smoke-gen E2E: 202 + status=succeeded + subject_count > 0 on E4_DM2 | manual curl + jq | see E2E runbook in RESEARCH | Manual-only (DEPLOY-LOG addendum) | ⬜ pending |
| 6 | TBD | 3 | — | — | `pancreas.cohort` (or `pancreas_results.cohort`) contains rows with `cohort_definition_id = gen_id + 100_000_000_000` | psql | `psql -U claude_dev -h localhost parthenon -Atc "SELECT COUNT(*) FROM pancreas.cohort WHERE cohort_definition_id = $EXPECTED_KEY"` → > 0 | Manual-only (DEPLOY-LOG addendum) | ⬜ pending |
| 7 | TBD | 3 | — | — | Non-FinnGen user cohort still writes with real `app.cohort_definitions.id` (< 100_000_000_000) | manual curl + psql | curl generate + `psql` verification per runbook | Manual-only (regression DEPLOY-LOG) | ⬜ pending |

### Phase 13 Invariants Preservation

| Invariant | Automated Command | Status |
|-----------|-------------------|--------|
| No new rows in `vocab.vocabulary` | `psql -U claude_dev -h localhost parthenon -Atc "SELECT COUNT(*) FROM vocab.vocabulary"` → same before/after 13.2 | ⬜ pending |
| `coverage_profile` populated on every endpoint | `psql -U claude_dev -h localhost parthenon -Atc "SELECT COUNT(*) FROM finngen.endpoint_definitions WHERE coverage_profile IS NULL"` → 0 | ⬜ pending |
| PANCREAS smoke-gen returns subject_count > 0 | SC-5 (above) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `backend/app/Models/App/FinnGenEndpointGeneration.php` — ADD `public const OMOP_COHORT_ID_OFFSET = 100_000_000_000;`
- [ ] `backend/app/Http/Controllers/Api/V1/FinnGen/EndpointBrowserController.php::generate()` — reorder: updateOrCreate generation first (with placeholder run_id), dispatch with `finngen_endpoint_generation_id`, backfill generation.run_id. Alternative (per Claude's discretion): `ALTER COLUMN run_id DROP NOT NULL` in the 13.2 migration (cleaner; 1-line).
- [ ] `darkstar/api/finngen/cohort_ops.R` — add `finngen_endpoint_generation_id` param handling; use numeric (NOT `L` int suffix) for the offset to avoid R int32 overflow; update 5 sprintf sites to use `%.0f` for large cohort_def_id.
- [ ] `backend/database/migrations/2026_04_20_NNNNNN_codify_phase_13_1_role_split_baseline.php` — idempotent migration with 3 grants; wrap `GRANT CREATE ON DATABASE` in `DO $$ EXCEPTION WHEN insufficient_privilege ... END $$` block.
- [ ] `backend/tests/Feature/FinnGen/EndpointGenerateCohortIdTest.php` — NEW Pest test asserting controller contract.
- [ ] `backend/tests/Feature/FinnGen/ImportEndpointsCommandTest.php` — 2-line PHPStan fix via `/** @var FinnGenConceptResolver&Mockery\MockInterface $mock */` docblock.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| PANCREAS smoke-gen end-to-end | SC 5 | Requires live Darkstar worker + PANCREAS data + admin token | See RESEARCH §Manual E2E Runbook (Steps 1-4) |
| `pancreas.cohort` keyed with offset ID | SC 6 | Requires completed E2E run from SC 5 | See RESEARCH §Manual E2E Runbook Step 5 |
| Non-FinnGen user cohort regression | SC 7 | Verifies legacy path still works post-change | See RESEARCH §Manual E2E Runbook Step 6 |
| `GRANT CREATE ON DATABASE` manual application | SC 3 workaround | `parthenon_migrator` may lack DB-owner rights in staging/prod | If migration's exception handler fires, run `sudo -u postgres psql -c "GRANT CREATE ON DATABASE <db> TO parthenon_migrator"` per runbook |

---

## Security Domain Checks

| Threat ID | STRIDE | Mitigation | Verification |
|-----------|--------|------------|--------------|
| T-13.2-S1 | Elevation of Privilege | `GRANT CREATE ON DATABASE` wrapped in exception handler; never fails open. Manual step documented for staging/prod. | Migration runs cleanly as `parthenon_migrator` on parthenon_testing; exception caught when privilege missing. |
| T-13.2-S2 | Tampering | 100B offset constant defined once in `FinnGenEndpointGeneration::OMOP_COHORT_ID_OFFSET` + mirrored in R with cross-reference comment; no env-drift risk. | grep for `100_000_000_000` + `100000000000` returns exactly 2 definitions + N referencing usages. |
| T-13.2-S3 | Information Disclosure | FinnGen cohort rows in `{source}.cohort` use keys ≥ 100B — any mistaken JOIN to `app.cohort_definitions` returns no rows (fail-closed by ID range mismatch). | psql assertion: `SELECT COUNT(*) FROM app.cohort_definitions WHERE id > 100000000000` → 0 always. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (5 files listed above)
- [ ] No watch-mode flags
- [ ] Feedback latency < 45s quick / 3min full
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
