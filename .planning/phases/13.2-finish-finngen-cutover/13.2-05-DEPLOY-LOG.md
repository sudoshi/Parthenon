# Phase 13.2 Plan 05 Deploy Log (DEV)

**Executed:** 2026-04-17T17:20:00Z → 2026-04-17T17:35:00Z (≈15 min wall clock, ≈2 min maintenance window)
**Operator:** Claude Code (continuation agent on branch `feature/phase-13.2-finngen-cutover`)
**Target DB:** `parthenon` on beastmode host PG17 (verified via `docker inspect parthenon-php`)
**Branch HEAD at start:** `ebfacfb3c` (docs(13.2): mark plans 01-04 complete)
**Migrations applied in this session:**
- `2026_04_20_000100_finngen_endpoint_generations_run_id_nullable`
- `2026_04_20_000200_codify_phase_13_1_role_split_baseline`
- `2026_04_20_000400_finngen_endpoint_generations_cohort_definition_id_nullable`

Note: `2026_04_20_000300_grant_usage_on_app_to_finngen_roles` was already `[102] Ran` from a prior Plan 06 execution — out-of-order ordering captured as Deviation 1 below.

---

## Summary

| Phase | Result |
|-------|--------|
| CHECKPOINT Task 1 (pre-deploy state) | PASS — completed by prior agent (see `<completed_by_prior_agents>`) |
| Step A (stale EUNOMIA run cleanup) | PASS — 3 stale runs marked failed by prior agent; active count = 0 |
| Step B (migrations via individual `--path=` invocations) | PASS — all 3 pending migrations applied |
| Step B verification (run_id nullable, tableowner, has_database_privilege) | PASS 3/3 |
| Step C (`./deploy.sh --openapi` + tsc + vite build) | PASS — types regenerated, TSC_PASS, vite build exit 0 |
| Step D B2 (E4_DM2 generate 202 — SC-2 evidence) | PASS — `finngen_endpoint_generation_id=1`, `cohort_definition_id=null` |
| Step D B3 (poll to terminal status) | PASS — `succeeded` on first 5s poll (≈3s total) |
| Step D B4 SC-5 (status=succeeded + subject_count > 0) | PASS — subject_count=135 |
| Step D B5 SC-6 (offset-keyed cohort rows) | PASS — `pancreas_results.cohort` @ 100000000001 = 135 |
| Step D B5 T-13.2-S3 invariant (no `app.cohort_definitions.id > 100B`) | PASS — 0 |
| Step E SC-7 (non-FinnGen regen legacy path) | PASS — cohort 225 regen via `GenerateCohortJob` completed in 2s, writes use real id (no offset) |
| Phase 13 invariant: `coverage_profile IS NULL` count | PASS — 0 |

---

## Step A — Stale Run Cleanup (prior agent)

Per `<completed_by_prior_agents>` context, a prior continuation agent marked 3 stale EUNOMIA runs as `failed` to satisfy the 0-active-runs precondition. Confirmed on resume:

```
psql -Atc "SELECT COUNT(*) FROM finngen.runs WHERE status IN ('queued','running','canceling')" → 0
```

Stale rows now terminal:
```
 01kpfbtd487zdrs3d8gnytgt11 | failed
 01kpfby0dthggj0bkr3eewky5w | failed
 01kpfchhyz33y8njpnj3jy8gz4 | failed
```

---

## Step B — Migration Execution

### Pre-migration state

```
SELECT migration FROM app.migrations WHERE migration LIKE '2026_04_20_0%' ORDER BY migration;
→ 2026_04_20_000300_grant_usage_on_app_to_finngen_roles  (only — applied out-of-order in Plan 06)

SELECT is_nullable FROM information_schema.columns
 WHERE table_schema='finngen' AND table_name='endpoint_generations' AND column_name='run_id';
→ NO
```

### Maintenance window

```
docker compose exec -T php php artisan down --secret="phase132cutover"
   INFO  Application is now in maintenance mode.

docker compose exec -T horizon php artisan horizon:pause
   INFO  Sending USR2 signal to processes.
```

### Deviation 2: `./deploy.sh --db` blocked by production guard

First attempt (`./deploy.sh --db`) was blocked by `AppServiceProvider::guardDangerousConsoleCommands('migrate')`:

```
RuntimeException: Refusing to run [migrate] without --path= against protected database [parthenon].
  Bare migrate can re-run ALL migrations and destroy data (see 2026-03-30 incident).
```

**Root cause:** `deploy.sh --db` runs bare `php artisan migrate --force` (line 478), but the 2026-03-30 data-loss incident (`feedback_never_migrate_force.md`) installed a guard that requires explicit `--path=` against protected DBs.

**Auto-fix (Rule 3):** applied each migration via explicit `--path=` invocation using the migrator role env override (mirroring deploy.sh's internal `MIG_USER`/`MIG_PW` logic):

```
docker compose exec -T -e DB_USERNAME=parthenon_migrator -e DB_PASSWORD=<redacted> \
  php php artisan migrate --path=database/migrations/2026_04_20_000100_finngen_endpoint_generations_run_id_nullable.php --force
   INFO  Running migrations.
   2026_04_20_000100_finngen_endpoint_generations_run_id_nullable . 6.21ms DONE

docker compose exec -T -e DB_USERNAME=parthenon_migrator -e DB_PASSWORD=<redacted> \
  php php artisan migrate --path=database/migrations/2026_04_20_000200_codify_phase_13_1_role_split_baseline.php --force
   INFO  Running migrations.
   2026_04_20_000200_codify_phase_13_1_role_split_baseline ........ 3.92ms DONE

docker compose exec -T -e DB_USERNAME=parthenon_migrator -e DB_PASSWORD=<redacted> \
  php php artisan migrate --path=database/migrations/2026_04_20_000400_finngen_endpoint_generations_cohort_definition_id_nullable.php --force
   INFO  Running migrations.
   2026_04_20_000400_finngen_endpoint_generations_cohort_definition_id_nullable  3.15ms DONE
```

Total DDL runtime: **13.28ms** (all three migrations combined).

### Post-migration state

```
SELECT migration FROM app.migrations WHERE migration LIKE '2026_04_20_0%' ORDER BY migration;
→ 2026_04_20_000100_finngen_endpoint_generations_run_id_nullable
  2026_04_20_000200_codify_phase_13_1_role_split_baseline
  2026_04_20_000300_grant_usage_on_app_to_finngen_roles
  2026_04_20_000400_finngen_endpoint_generations_cohort_definition_id_nullable
```

### DDL effect verification

| Check | Expected | Actual | Pass? |
|-------|----------|--------|:-----:|
| `finngen.endpoint_generations.run_id` is_nullable | `YES` | `YES` | PASS |
| `app.cohort_definitions` tableowner | `parthenon_migrator` | `parthenon_migrator` | PASS |
| `has_database_privilege('parthenon_migrator', 'parthenon', 'CREATE')` | `t` | `t` | PASS |

### Resume

```
docker compose exec -T horizon php artisan horizon:continue
   INFO  Sending CONT signal to processes.

docker compose exec -T php php artisan up
   INFO  Application is now live.
```

### Deviation 3: `/api/health` path (not `/api/v1/health`)

Initial runbook used `http://localhost:8082/api/v1/health` which returned 404. Correct path on this deploy is `http://localhost:8082/api/health` → returns 200. Health alias route is in `routes/api.php` above the `v1` group.

```
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8082/api/health     → 200
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8082/api/v1/sources → 401  (auth required, not 503)
```

---

## Step C — OpenAPI + Frontend Build

```
./deploy.sh --openapi
  ✓ API docs + OpenAPI spec generated → public/docs/
  ✓ Spec exported → backend/api.json
  ✓ Types generated → frontend/src/types/api.generated.ts

docker compose exec -T node sh -c "cd /app && npx tsc --noEmit" → TSC_PASS
docker compose exec -T node sh -c "cd /app && npx vite build"
  ✓ built in 1.94s
  VITE_RESULT=0
```

Smoke checks green (`/`, `/login`, `/jobs`, `/sanctum/csrf-cookie`, `/api/v1/nonexistent-endpoint`).

---

## Step D — E2E Smoke-Gen (SC-5 / SC-6)

### D1 — Admin token (redacted)

Acquired via artisan tinker for user id=117 (`admin@acumenus.net`). Token length=53. Token NOT logged. Token file `/tmp/13.2-05-token` was chmod 600 and deleted at end of session.

### D2 — E4_DM2 generate dispatch

**Endpoint:** `POST /api/v1/finngen/endpoints/E4_DM2/generate`
**Body:** `{"source_key":"PANCREAS","overwrite_existing":true}`
**Headers:** `X-Idempotency-Key: 13.2-05-1745083364`
**Status:** 202

**Key response fields (`data.run`):**
- `run.id` = `01kpgsveey5h4gbas4syk8j016`
- `run.params.cohort_definition_id` = `null` **SC-2 green**
- `run.params.finngen_endpoint_generation_id` = `1` **SC-2 green**
- `run.user_id` = `117`
- `run.source_key` = `PANCREAS`
- `run.analysis_type` = `endpoint.generate`

**Derived:** `EXPECTED_COHORT_KEY = GEN_ID + 100_000_000_000 = 100000000001`

### D3 — Poll timeline

```
[13:22:54] #1 status=succeeded    ← terminal on first poll
```

Run completed between `created_at=2026-04-18T21:22:41Z` and `finished_at=2026-04-18T21:22:47Z` — **6 seconds end-to-end**.

### D4 — SC-5 terminal state

```json
{
  "id": "01kpgsveey5h4gbas4syk8j016",
  "status": "succeeded",
  "summary": {
    "subject_count": 135,
    "cohort_definition_id": 100000000001
  },
  "artifacts": [],
  "darkstar_job_id": "finngen.endpoint.generate_20260418172244.653291_33049",
  "started_at":  "2026-04-18T21:22:44.000000Z",
  "finished_at": "2026-04-18T21:22:47.000000Z"
}
```

- `status = "succeeded"` — **SC-5 A green**
- `summary.subject_count = 135 > 0` — **SC-5 B green**
- `summary.cohort_definition_id = 100000000001` = `GEN_ID + 100B` — confirms R worker computed offset correctly

### D5 — SC-6 cohort-key verification

```
SELECT COUNT(*) FROM pancreas.cohort           WHERE cohort_definition_id = 100000000001  → 0
SELECT COUNT(*) FROM pancreas_results.cohort   WHERE cohort_definition_id = 100000000001  → 135
```

**Write target identified: `pancreas_results.cohort`** (RESEARCH §Open Question 1 resolved — FinnGen path writes to the `_results` schema, not the raw CDM schema). Row count (135) matches `summary.subject_count` exactly. **SC-6 green.**

### D6 — T-13.2-S3 fail-closed invariant

```
SELECT COUNT(*) FROM app.cohort_definitions WHERE id > 100000000000 → 0
```

**T-13.2-S3 green** — no collision vector between FinnGen offset keys and real `app.cohort_definitions.id`.

---

## Step E — SC-7 Non-FinnGen Regression

### First attempt — cohort 249 (target from orchestrator prompt)

Controller validation requires `source_id` (integer), not `source_key` — adjusted request body to `{"source_id": 58}` (PANCREAS).

```
POST /api/v1/cohort-definitions/249/generate  {"source_id": 58}
→ 202 { "data": { "id": 648, "cohort_definition_id": 249, ...} }
```

Job ran immediately (Horizon log: `App\Jobs\Cohort\GenerateCohortJob ... 27.44ms DONE`). However, job 649 was the real dispatched job and **failed** with:

```
fail_message: "PrimaryCriteria is required in cohort expression."
```

Cohort 249 is a test artifact ("Smoke 260416-postfix UNION 222+223 (overwrite)") with a malformed expression. The 237 rows in `pancreas_results.cohort @ 249` pre-existed from earlier runs — this was not a clean regression probe.

### Second attempt — cohort 225 "KRAS Mutant PDAC" (proven-good target)

Queried `app.cohort_generations` for a non-FinnGen cohort with a recent successful PANCREAS generation:

```
id=225, name='KRAS Mutant PDAC', domain='oncology', last_person_count=285 (2026-04-04)
```

Pre-regen state: `pancreas_results.cohort @ 225 = 285`

```
POST /api/v1/cohort-definitions/225/generate  {"source_id": 58}
→ 202 { "data": { "id": 650, "cohort_definition_id": 225, "status": "queued" } }
```

**Horizon log confirms job execution:**
```
parthenon-horizon | 2026-04-18 17:29:38 App\Jobs\Cohort\GenerateCohortJob ... RUNNING
parthenon-horizon | 2026-04-18 17:29:41 App\Jobs\Cohort\GenerateCohortJob ......... 2s DONE
```

### Post-regen state

```
SELECT COUNT(*) FROM pancreas_results.cohort WHERE cohort_definition_id = 225 → 285
SELECT COUNT(*) FROM app.cohort_definitions  WHERE id = 225 AND id < 100000000000 → 1
```

- Legacy `GenerateCohortJob` completed in 2s ✓
- Rows written under real id `225` (< 100B), NOT offset key `100000000225` ✓
- `grep -n "COHORT_ID_OFFSET\|100_000_000_000" backend/app/Jobs/Cohort/GenerateCohortJob.php backend/app/Services/Cohort/*.php` → 0 matches (legacy path has zero offset references)

**SC-7 green.** The legacy non-FinnGen cohort path continues to use real `app.cohort_definitions.id` with no offset transformation.

**Known stale status:** `app.cohort_generations.id=650.status` remained `queued` after the job completed successfully. The `GenerateCohortJob` appears not to update the `cohort_generations` row on completion. This is a pre-existing behavior unrelated to Phase 13.2 (same pattern visible on job 648 which ran 27.44ms and also stayed `queued`). Tracked separately in `.planning/phases/13.2-finish-finngen-cutover/deferred-items.md` if present.

---

## Invariant Preservation

| Invariant | Command | Actual | Pass? |
|-----------|---------|--------|:-----:|
| Phase 13 GENOMICS-12a: PANCREAS smoke-gen success with subject_count > 0 | Steps D2-D4 | status=succeeded, subject_count=135 | **CLOSED** |
| Phase 13 coverage_profile populated on every endpoint | `SELECT COUNT(*) FROM finngen.endpoint_definitions WHERE coverage_profile IS NULL` | 0 | PASS |
| T-13.2-S3 offset/real-id collision | `SELECT COUNT(*) FROM app.cohort_definitions WHERE id > 100000000000` | 0 | PASS |
| No new rows in `vocab.vocabulary` | N/A — no vocab changes in 13.2 | — | N/A |

---

## SC Coverage (7 SCs from ROADMAP §Phase 13.2)

| SC | Closed By | Evidence |
|----|-----------|----------|
| 1 (R worker contract) | Plan 03 + Step D | R source bind-mounted; runbook observed succeeded run with `summary.cohort_definition_id=100000000001` — confirms offset computation in R |
| 2 (controller contract) | Plan 03 + Step D2 | Response body has `finngen_endpoint_generation_id=1` ≠ null AND `cohort_definition_id=null` |
| 3 (role-split codification) | Plan 02 + Step B | Migration `2026_04_20_000200` applied; `tableowner=parthenon_migrator` and `has_database_privilege CREATE=t` confirmed |
| 4 (Pest suite green) | Plan 04 | `13.2-04-PEST-LOG.md` captured green run on `parthenon_testing` |
| 5 (PANCREAS smoke-gen) | Steps D2-D4 | status=succeeded, subject_count=135 |
| 6 (offset-keyed cohort rows) | Step D5 | `pancreas_results.cohort @ 100000000001 = 135` |
| 7 (legacy path regression) | Step E (cohort 225) | Non-FinnGen regen via `GenerateCohortJob` completed 2s; rows preserved at real id=225 |

**7/7 SCs green.**

---

## Deviations / Manual Interventions

| # | Rule | Issue | Fix | Persistent env change? |
|---|------|-------|-----|:----------------------:|
| **D-01** | Rule 3 | `2026_04_20_000300_grant_usage_on_app_to_finngen_roles` already `[102] Ran` from Plan 06 — out-of-order application | No action needed; migration is idempotent (DO block with exception handler). `app.migrations` now shows all 4 rows in lexical order but non-contiguous batch numbers. | No — cosmetic only |
| **D-02** | Rule 3 | `./deploy.sh --db` blocked by `AppServiceProvider::guardDangerousConsoleCommands` (post-2026-03-30 guard requires `--path=`) | Applied each migration via explicit `--path=` with `DB_USERNAME/DB_PASSWORD` env override (mirrors `deploy.sh` internal logic on lines 470–479) | No — runbook knowledge (future plans should use `--path=` directly for sensitive migrations) |
| **D-03** | Rule 1 | Runbook's `GET /api/v1/health` probe returns 404 (`/api/v1/health` is not a registered route; health alias is at `/api/health`) | Used `/api/health` → 200 | No — documentation correction |
| **D-04** | Rule 3 | Orchestrator dispatch described docker stack bound to codex-i18n-pr5 sibling worktree (mid-chain rebinding) | Stack rebound to `/home/smudoshi/Github/Parthenon/backend` by orchestrator before executor resume. Verified via `docker inspect parthenon-php` → `project.working_dir = /home/smudoshi/Github/Parthenon` | No — orchestration detail |
| **D-05** | Rule 3 | Orchestrator-specified cohort 249 for regression was a test artifact with malformed expression (`PrimaryCriteria is required`) | Picked cohort 225 "KRAS Mutant PDAC" — non-FinnGen (domain=oncology), proven successful PANCREAS history (person_count=285) | No — test-data selection |
| **D-06** | N/A (Scope Boundary) | `GenerateCohortJob` does not update `app.cohort_generations.status` on completion (both jobs 648 and 650 stayed `queued` despite successful execution) | Pre-existing behavior outside 13.2 scope. Evidence of completion from Horizon log only. SC-7 passes via row-count verification (not status check). | No — deferred investigation |

---

## Commits

- Plan 05 execution commit (this DEPLOY-LOG): pending
- Plan 05 SUMMARY commit: pending

Previous Plan 01-04 commits referenced in chain; Plan 07 (out-of-order 000300) captured in `13.2-07-DEPLOY-LOG.md` if present.
