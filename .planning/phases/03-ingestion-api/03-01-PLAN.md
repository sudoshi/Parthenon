---
phase: 03-ingestion-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/ingestion/api/ingestionApi.ts
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
  - INGEST-03
  - INGEST-04

must_haves:
  truths:
    - "All 18 API functions in ingestionApi.ts unwrap the Laravel {data: T} envelope"
    - "IngestionDashboardPage renders a job list array (not [object Object])"
    - "UploadPage receives a job object with .id after upload and navigates to it"
    - "JobDetailPage accesses .status and .current_step on a real IngestionJob object"
    - "MappingReviewPage receives arrays from fetchMappings/fetchCandidates"
    - "SchemaMappingPage receives arrays from fetchSchemaMapping/suggestSchemaMapping"
  artifacts:
    - path: "frontend/src/features/ingestion/api/ingestionApi.ts"
      provides: "All ingestion API functions with correct envelope unwrapping"
      contains: "data.data ?? data"
  key_links:
    - from: "frontend/src/features/ingestion/pages/IngestionDashboardPage.tsx"
      to: "ingestionApi.fetchJobs"
      via: "TanStack useQuery"
      pattern: "fetchJobs.*returns.*IngestionJob\\[\\]"
    - from: "frontend/src/features/ingestion/pages/UploadPage.tsx"
      to: "ingestionApi.uploadFile"
      via: "TanStack useMutation onSuccess"
      pattern: "job\\.id"
    - from: "frontend/src/features/ingestion/pages/JobDetailPage.tsx"
      to: "ingestionApi.fetchJob"
      via: "TanStack useQuery"
      pattern: "job\\.status.*job\\.current_step"
---

<objective>
Fix all 18 API functions in ingestionApi.ts to correctly unwrap Laravel's {data: T} response envelope, making all ingestion pages render real data instead of broken envelope objects.

Purpose: The ingestion module is the only feature module that does not unwrap the Laravel response envelope. Every other module (studies, cohorts, concept-sets, genomics) uses the `data.data ?? data` pattern. This single-file fix unblocks the entire ingestion workflow.

Output: A corrected ingestionApi.ts where every function returns unwrapped data.
</objective>

<execution_context>
@/home/smudoshi/.claude/get-shit-done/workflows/execute-plan.md
@/home/smudoshi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@frontend/src/features/ingestion/api/ingestionApi.ts
@frontend/src/features/concept-sets/api/conceptSetApi.ts (reference pattern)
@frontend/src/lib/api-client.ts (toLaravelPaginated helper)

<interfaces>
<!-- Correct unwrapping pattern used by all other modules -->
From frontend/src/features/concept-sets/api/conceptSetApi.ts:
```typescript
// Single-resource pattern:
export async function getConceptSet(id: number): Promise<ConceptSet> {
  const { data } = await apiClient.get(`${BASE}/${id}`);
  return data.data ?? data;  // <-- unwrap envelope
}

// Paginated list pattern:
export async function getConceptSets(params?): Promise<PaginatedResponse<ConceptSet>> {
  const { data } = await apiClient.get(BASE, { params });
  return toLaravelPaginated<ConceptSet>(data);  // <-- paginated unwrap
}
```

From frontend/src/lib/api-client.ts:
```typescript
export function toLaravelPaginated<T>(response: {
  data: T[]; total: number; current_page: number; per_page: number;
}): { items: T[]; total: number; page: number; limit: number };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add envelope unwrapping to all ingestionApi.ts functions</name>
  <files>frontend/src/features/ingestion/api/ingestionApi.ts</files>
  <action>
For every function in ingestionApi.ts, change `return data;` to `return data.data ?? data;` to unwrap Laravel's `{data: T}` response envelope. This matches the pattern used by conceptSetApi.ts, cohortApi.ts, genomicsApi.ts, and all other modules.

Functions to fix (18 total):
1. `uploadFile` -- returns IngestionJob (line 29: `return data;` -> `return data.data ?? data;`)
2. `fetchJobs` -- returns IngestionJob[] (line 39)
3. `fetchJob` -- returns IngestionJob (line 44)
4. `fetchProfile` -- returns SourceProfile (line 51)
5. `retryJob` -- returns IngestionJob (line 62)
6. `fetchMappings` -- returns ConceptMapping[] (line 73)
7. `fetchMappingStats` -- returns MappingStats (line 80)
8. `submitReview` -- returns ConceptMapping (line 91)
9. `submitBatchReview` -- returns {reviewed: number} (line 102)
10. `fetchCandidates` -- returns ConceptMapping["candidates"] (line 112)
11. `searchMappings` -- returns MappingSearchResult (line 131)
12. `suggestSchemaMapping` -- returns SchemaMapping[] (line 141)
13. `fetchSchemaMapping` -- returns SchemaMapping[] (line 149)
14. `updateSchemaMapping` -- returns SchemaMapping[] (line 160)
15. `confirmSchemaMapping` -- returns {confirmed: number} (line 169)
16. `fetchValidation` -- returns ValidationResult[] (line 179)
17. `fetchValidationSummary` -- returns ValidationSummary (line 188)

Note: `deleteJob` (line 54-56) returns void and has no `return data` -- skip it.

Do NOT change the TypeScript generic type parameters on apiClient calls. Only change the return statement from `return data;` to `return data.data ?? data;`.

Do NOT add toLaravelPaginated to fetchJobs -- the dashboard page uses it as a plain array (`.map()` on the result), not paginated. The backend index route likely returns all jobs without pagination since ingestion jobs are low-volume.
  </action>
  <verify>
    <automated>cd /home/smudoshi/Github/Parthenon/frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 17 data-returning functions in ingestionApi.ts use `data.data ?? data` pattern. TypeScript compilation passes with no new errors. The `deleteJob` void function is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Verify consumer pages have no type errors</name>
  <files>frontend/src/features/ingestion/api/ingestionApi.ts</files>
  <action>
Run full TypeScript check to confirm no type errors in consumer pages:
- IngestionDashboardPage.tsx (calls fetchJobs, expects IngestionJob[])
- UploadPage.tsx (calls uploadFile, accesses job.id in onSuccess)
- JobDetailPage.tsx (calls fetchJob, accesses job.status, job.current_step)
- MappingReviewPage.tsx (calls fetchMappings, fetchMappingStats, submitReview, submitBatchReview, fetchCandidates, searchMappings)
- SchemaMappingPage.tsx (calls fetchSchemaMapping, suggestSchemaMapping, updateSchemaMapping, confirmSchemaMapping, fetchProfile)

If any type errors arise from the unwrapping (e.g., `data.data` not existing on the TS type), the fix is to remove the explicit generic type parameter from the apiClient call so `data` is typed as `any`, allowing the `data.data ?? data` pattern to work. This is the same approach used in conceptSetApi.ts (no generic on apiClient.get).

Run: `cd frontend && npx tsc --noEmit`

Expected: 0 new errors from ingestion files. Pre-existing errors in other files are acceptable.
  </action>
  <verify>
    <automated>cd /home/smudoshi/Github/Parthenon/frontend && npx tsc --noEmit 2>&1 | grep -c "ingestion" || echo "0 ingestion errors"</automated>
  </verify>
  <done>TypeScript compilation produces zero errors referencing any file in features/ingestion/. All consumer pages type-check cleanly against the updated API functions.</done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` -- no errors in ingestion files
2. `grep -c "data.data ?? data" frontend/src/features/ingestion/api/ingestionApi.ts` -- should return 17 (all data-returning functions)
3. `grep "return data;" frontend/src/features/ingestion/api/ingestionApi.ts` -- should return 0 matches (no raw returns left)
</verification>

<success_criteria>
- All 17 data-returning API functions in ingestionApi.ts use `data.data ?? data`
- `npx tsc --noEmit` passes with no ingestion-related errors
- Pattern matches what conceptSetApi.ts, cohortApi.ts, and genomicsApi.ts use
</success_criteria>

<output>
After completion, create `.planning/phases/03-ingestion-api/03-01-SUMMARY.md`
</output>
