---
phase: 13.1
plan: 13.1-04
title: Pest test migration + expanded IsolationRollbackTest round-trip
status: completed
completed: 2026-04-17
---

# Plan 13.1-04 Summary

## What Was Built

- **3 Phase 13 Pest tests rewritten** off `CohortDefinition`/`CohortDomain::FINNGEN_ENDPOINT` fixtures onto `EndpointDefinition` + `finngen.*` tables:
  - `backend/tests/Feature/FinnGen/CoverageProfilePopulationTest.php`
  - `backend/tests/Feature/FinnGen/ImportEndpointsOverwriteTest.php`
  - `backend/tests/Feature/FinnGen/ImportEndpointsCommandTest.php`
- **4th file from RESEARCH inventory (`WorkbenchSessionTest.php`) intentionally left untouched** — its `CohortDefinition` usage is legitimate promote-match boundary crossing per CONTEXT D-13. No rewrite needed.
- **IsolationRollbackTest.php expanded** from the Wave 0 schema-only stub (48 lines) to a full 3-phase round-trip (152 lines) covering migrate → rollback → migrate with row-count + column + schema assertions at each boundary.
- **Mandatory `afterAll(migrate:fresh)` cleanup hook** added to IsolationRollbackTest — prevents suite poisoning when this test runs before other FinnGen tests.

## Commits

- `1b91c9b7a` — test(13.1-04): rewrite 3 Phase 13 FinnGen tests to use EndpointDefinition + finngen.* tables
- `28c8a970e` — test(13.1-04): expand IsolationRollbackTest with row-count round-trip + mandatory migrate:fresh cleanup

## Verification

- Pint --test: PASS across all 4 files
- PHPStan level 8: clean on 3 of 4 files (`ImportEndpointsCommandTest` retains 4 pre-existing Mockery type errors out of scope for this plan)
- PHP syntax: clean on all files

## Auto-Fixes (Rules 1-3)

- 1 × Rule 1 — PHPStan `glob()` narrowing
- 3 × Rule 2:
  - admin user seeding precondition
  - rehydrated-name equality alongside row count
  - symmetric `coverage_profile` absence assertions at Phase A + C

No Rule 4 architectural checkpoints.

## Deferred to Plan 13.1-05

- Runtime Pest execution — the migration has not yet been applied to the dev DB; Plan 13.1-05 runs `./deploy.sh --db` + Pest as part of the deploy runbook checkpoint.

## Success Criteria Mapped

- SC 8 (functional rollback): covered by IsolationRollbackTest (now a full round-trip, not just a schema stub).
- All 4 tests compile and static-check clean; runtime green signal awaits Plan 05.
