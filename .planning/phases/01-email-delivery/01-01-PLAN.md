---
phase: 01-email-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/.env
  - backend/.env.example
  - backend/app/Mail/TempPasswordMail.php
  - .claude/rules/auth-system.md
autonomous: false
requirements:
  - EMAIL-01
  - EMAIL-02
  - EMAIL-03

must_haves:
  truths:
    - ".env uses RESEND_KEY (not RESEND_API_KEY) matching config/services.php"
    - "Registration sends a temp password email that arrives via Resend"
    - "Forgot password sends a new temp password email that arrives via Resend"
  artifacts:
    - path: "backend/.env"
      provides: "Correct RESEND_KEY env variable"
      contains: "RESEND_KEY="
    - path: "backend/.env.example"
      provides: "Correct env example for other developers"
      contains: "RESEND_KEY="
    - path: "backend/app/Mail/TempPasswordMail.php"
      provides: "Accurate docblock referencing RESEND_KEY"
      contains: "RESEND_KEY"
    - path: ".claude/rules/auth-system.md"
      provides: "Updated project rules reflecting RESEND_KEY variable name"
      contains: "RESEND_KEY"
  key_links:
    - from: "backend/.env"
      to: "backend/config/services.php"
      via: "env('RESEND_KEY') reads from .env"
      pattern: "RESEND_KEY="
    - from: "backend/config/services.php"
      to: "vendor resend mail transport"
      via: "services.resend.key config value"
      pattern: "resend.*key"
---

<objective>
Fix the RESEND_KEY environment variable mismatch so email delivery works, then verify both registration and forgot-password flows send real emails.

Purpose: Users currently never receive temp password emails because `.env` defines `RESEND_API_KEY` but Laravel reads `RESEND_KEY` via `config/services.php` line 28. The Resend transport gets a null key and silently fails.

Output: Working email delivery for both auth flows.
</objective>

<execution_context>
@/home/smudoshi/.claude/get-shit-done/workflows/execute-plan.md
@/home/smudoshi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/rules/auth-system.md

<interfaces>
<!-- From backend/config/services.php line 27-29 (DO NOT MODIFY this file): -->
'resend' => [
    'key' => env('RESEND_KEY'),
],

<!-- From backend/app/Mail/TempPasswordMail.php (only docblock update allowed): -->
class TempPasswordMail extends Mailable {
    public function __construct(
        public readonly string $userName,
        public readonly string $tempPassword,
    ) {}
}

<!-- From vendor MailManager (read-only context): -->
<!-- Laravel reads $config['key'] ?? $this->app['config']->get('services.resend.key') -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix RESEND_KEY env variable mismatch and update project docs</name>
  <files>backend/.env, backend/.env.example, backend/app/Mail/TempPasswordMail.php, .claude/rules/auth-system.md</files>
  <action>
1. In `backend/.env`: Rename `RESEND_API_KEY` to `RESEND_KEY` on line 80. Keep the existing value intact. Do NOT expose or log the API key value.

2. In `backend/.env.example`: Rename `RESEND_API_KEY` to `RESEND_KEY` on line 84 (value stays empty).

3. In `backend/app/Mail/TempPasswordMail.php`: Update ONLY the docblock comment on line 15 from `RESEND_API_KEY=your-key` to `RESEND_KEY=your-key`. Do NOT modify any code in this file -- only the comment.

4. In `.claude/rules/auth-system.md`: Update two references from `RESEND_API_KEY` to `RESEND_KEY`:
   - Rule #7 (line 54): Change `(use RESEND_API_KEY env var)` to `(use RESEND_KEY env var)`
   - Resend Configuration section (line 65): Change `RESEND_API_KEY in backend/.env` to `RESEND_KEY in backend/.env`
   These updates keep project documentation consistent with the actual env variable name that config/services.php reads.

5. Recreate the PHP container so the new env var is loaded (Docker env_file loads at creation time, not restart time per CLAUDE.md gotcha #8):
   ```bash
   cd /home/smudoshi/Github/Parthenon && docker compose up -d php
   ```

6. Verify the config resolves correctly inside the container:
   ```bash
   docker compose exec php php artisan tinker --execute="echo config('services.resend.key') ? 'KEY_PRESENT' : 'KEY_MISSING';"
   ```
   Expected output: `KEY_PRESENT`

CRITICAL constraints:
- Do NOT modify `backend/config/services.php` -- it already reads `RESEND_KEY` correctly
- Do NOT modify any auth controllers or email templates per auth-system rules
- Do NOT commit the .env file (it contains secrets)
- Do NOT log or echo the actual API key value
  </action>
  <verify>
    <automated>cd /home/smudoshi/Github/Parthenon && docker compose exec php php artisan tinker --execute="echo config('services.resend.key') ? 'KEY_PRESENT' : 'KEY_MISSING';" && docker compose exec php php artisan tinker --execute="echo config('mail.default') === 'resend' ? 'MAILER_OK' : 'MAILER_WRONG';"</automated>
  </verify>
  <done>
- backend/.env has RESEND_KEY (not RESEND_API_KEY) with the API key value
- backend/.env.example has RESEND_KEY= (empty placeholder)
- TempPasswordMail.php docblock references RESEND_KEY
- .claude/rules/auth-system.md Rule #7 and Resend Configuration section reference RESEND_KEY
- PHP container rebuilt and config('services.resend.key') returns the key
- config('mail.default') returns 'resend'
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify email delivery end-to-end</name>
  <what-built>Fixed the RESEND_KEY env variable so Laravel can authenticate with the Resend email API. Both registration and forgot-password flows use TempPasswordMail which now has a valid API key.</what-built>
  <how-to-verify>
Test both email flows that use TempPasswordMail:

**Test 1 — Registration (EMAIL-02):**
1. Open http://localhost:8082 (or https://parthenon.acumenus.net)
2. Click "Request access" on the login page
3. Fill in: name, your real email address, phone (optional)
4. Submit the registration form
5. Check your email inbox for "Your Parthenon access credentials" from noreply@acumenus.net
6. Confirm the email contains a temp password

**Test 2 — Forgot Password (EMAIL-03):**
1. On the login page, click "Forgot password?"
2. Enter the email address you just registered with
3. Check your email inbox for a new "Your Parthenon access credentials" email
4. Confirm the email contains a NEW temp password (different from the first)

If emails arrive: both flows work. Type "approved".
If emails do NOT arrive: describe what happened (error message, no response, etc.).
  </how-to-verify>
  <resume-signal>Type "approved" if both emails arrived, or describe the issue</resume-signal>
</task>

</tasks>

<verification>
- `config('services.resend.key')` returns a non-null value inside the PHP container
- `config('mail.default')` returns 'resend' inside the PHP container
- Registration flow sends email via Resend (arrives in inbox)
- Forgot password flow sends email via Resend (arrives in inbox)
</verification>

<success_criteria>
- EMAIL-01: .env variable name matches what config/services.php reads
- EMAIL-02: Registration triggers temp password email that arrives
- EMAIL-03: Forgot password triggers temp password email that arrives
</success_criteria>

<output>
After completion, create `.planning/phases/01-email-delivery/01-01-SUMMARY.md`
</output>
